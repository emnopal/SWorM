package loader

import "fmt"

type FileLoader struct {
	FilePath string
}

func (fl *FileLoader) Load() (string, error) {
	content, err := loadFile(fl.FilePath)
	if err != nil {
		return "", fmt.Errorf("failed to load file: %w", err)
	}
	return content, nil
}
